# Render blueprint for the AngkorCraft backend.
# In Render: New + → Blueprint → connect this repo. Then set the secret env
# vars (marked sync:false) in the dashboard, and set CORS_ORIGINS to
# "https://your-app.vercel.app,https://*.vercel.app" (the wildcard covers Vercel
# preview deploys). UptimeRobot should ping https://<service>.onrender.com/health.
services:
  - type: web
    name: angkorcraft-api
    runtime: node
    rootDir: backend
    plan: free
    # --include=dev forces devDependencies (typescript) to install even though
    # Render sets NODE_ENV=production — otherwise `tsc` isn't found and the build fails.
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: '20'
      - key: CORS_ORIGINS
        sync: false
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false
      # Optional — enables best-quality Khmer auto-translation. Leave unset to
      # use the free fallback provider.
      - key: GOOGLE_TRANSLATE_API_KEY
        sync: false
      # Optional — automated image moderation (e.g. 'aws_rek' add-on or
      # 'manual'). Leave unset for reporting-based moderation only.
      - key: CLOUDINARY_MODERATION
        sync: false
